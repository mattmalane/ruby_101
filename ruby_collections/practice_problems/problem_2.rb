['ant', 'bat', 'caterpillar'].count do |str|
  str.length < 4
end

#read ruby docs to find out what the method count does
#if the block evaluates truthy then the count method will count the true elements